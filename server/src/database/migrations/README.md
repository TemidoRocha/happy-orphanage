## to create the migration automatically

yarn typeorm migration:create -n create_orphanages

## define where to save the migrations on ormconfig.json

## to run the migrations

yarn typeorm migration:run
